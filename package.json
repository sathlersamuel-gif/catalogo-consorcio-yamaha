{
  "name": "catalogo-consorcio-yamaha",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check api.js && node --check client.js && node --check admin.js && node --check config.js && node --check seed.js && npm test"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
